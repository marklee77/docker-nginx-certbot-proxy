- move my_init.d from bin to sbin...
- need volume for dhparam and proper permissions
- default is the same as any host, but w/ default on listen line
- work on script to manipulate proxies
  -- add
    1. create no-https config (501 except for acme)
    2. restart nginx? or can it load w/out restart?
       /usr/sbin/nginx -s reload
    3. letsencrypt
    4. write new config w/ https
    5. re-start/reload
  -- delete
    1. delete
    2. restart
  -- disable
    - use sites-available / sites-enabled idiom
  -- set default
    - need to unset default in others (sed?)
    - reload

- daily cron job to renew
- volume for error pages
- support hsts / preload and hpkp

letsencrypt certonly --agree-tos --rsa-key-size 4096 --email webmaster@stillwell.me --webroot -d $domainname -w /var/www/$domainname/htdocs

http://letsencrypt.readthedocs.io/en/latest/using.html#certbot-command-line-options

